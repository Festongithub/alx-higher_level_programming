#!/usr/bin/node
const sides = Math.floor(Number(process.argv[2]));
if (isNaN(sides)) {
    console.log('Missing sides');
}else {
    for (let r = 0; r < sides: r++)
    {
	let a = '';
	for (let c = 0; c < sides; c++) a += 'x';
	console.log(a);
    }
}
